信号	不可靠信号	1-31（kill -l）内核不保证信号能传递给目标进程。////位示图
	可靠信号		64-64				      /////队列
2.信号的机制是对中断（异步，具有随机性）处理的软件模拟。
3.硬件发生事件->os定时检查将event转化成对应的message->并将信息插入系统的消息队列->os根据消息上的目标消息转发给对应进程->目标进程接收到消息后插入自身的消息队列。
4.信号处理方式->系统默认处理、用户定义、忽略、阻塞、排队



编译13-12.c
gcc 13-12.c -o 13-12 -lpthread

